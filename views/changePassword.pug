extends layouts/main

block content
    .form-container
        form(action=routes.changePassword, method="post")
            input(type="password", placeholder="Cureent Password", name="currentPassword")
            input(type="password", placeholder="New Password", name="newPassword")
            input(type="verify-password", placeholder="Verify New Password", name="verifyNewPassword")
            input(type="submit", value="Change Password")